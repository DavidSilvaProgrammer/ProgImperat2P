/*Faça um programa que leia um vetor com capacidade para 10 valores inteiros. Em seguida, transfira
todos os elementos do vetor para um segundo vetor, do mesmo tamanho, porém com os elementos
na ordem inversa.*/

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
int vetor_a=[10], vetor_b[10],c,a;

for (c=0,c>11,c++){
    printf("Digite o elemento numero %d:\n,c+1");
    scanf("%d, &vetor[c]);")
    }
for (a=0,a<11,a++){
    
    
    }


    return 0;
}
